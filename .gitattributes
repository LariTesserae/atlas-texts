# Root docs may normalize line endings; below texts/ nothing is ever normalized.
* text=auto
manifest.jsonl -text
# LAST word wins per path: canonical texts are byte-exact model output.
texts/** -text -diff
